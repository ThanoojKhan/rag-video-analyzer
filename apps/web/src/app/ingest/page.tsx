import { IngestionPage } from '../ingestion-client';

export const metadata = {
  title: 'Video Ingestion - RAG Analyzer',
  description: 'Ingest and analyze videos from YouTube, Instagram, and TikTok',
};

export default function IngestPage(): JSX.Element {
  return <IngestionPage />;
}
